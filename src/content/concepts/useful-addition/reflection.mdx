---
title: "Reflection"
layer: useful-addition
oneLiner: "Let the agent critique its own output against a goal or rubric and revise — a self-correction loop that trades extra model calls for higher-quality results on tasks where a first draft is rarely right."
prerequisites:
  - workflows-vs-agents
  - evaluation
related:
  - planning
  - failure-modes
interviewTags: ["reflection", "self-critique", "agents", "quality"]
status: stub
updated: 2026-07-19
---

> **Stub** — planned territory on the atlas. This concept will cover: the reflect →
> critique → revise loop (self-refinement, reflexion), why a model can often judge a draft
> better than it can produce one in a single pass, the connection to evaluation (the critic
> is an eval applied inline), the honest trade-offs (more latency and cost, no guarantee the
> critique is correct — a model can confidently "improve" a good answer into a worse one),
> and when reflection earns its cost versus when a better prompt or a fixed check is simpler.
> It builds on Workflows vs agents and Evaluation.
